import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    position: "absolute",
    top: 15,
    zIndex: 5,
    alignItems: "center",
    width: "100%",
  },
  logo: {
    height: 70,
    resizeMode: "contain",
  },
});

export default styles;
